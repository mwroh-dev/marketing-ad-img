node_modules/
dist/
.mcp-runtime/
*.log
.DS_Store

# runtime output (dev only) — never released
artifacts/

# consumer runtime state — never released (brand/competitors/collected ads live here)
.generate-ads-img/

# dev working files — never released
tasks/

# never commit raw credential-bearing material
**/*.cookies.json
**/credentials*.json

# dev-only working files — never released
IMPLEMENTATION_NOTES.md
.claude/

# local test sandboxes (copied consumer state for live subsession tests) — never released
samples/

# superpowers internal artifacts (brainstorming / plans / specs) — internal dev aid, NEVER shipped to service
docs/superpowers/
**/superpowers/
.superpowers/
