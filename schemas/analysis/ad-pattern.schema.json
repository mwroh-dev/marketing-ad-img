{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://marketing-img/schemas/ad-pattern.schema.json",
  "title": "AdPattern",
  "description": "L3 — per-persona aggregated ad composition pattern.",
  "type": "object",
  "additionalProperties": false,
  "required": ["product_id", "persona_id", "image_count", "composition_top_k", "text_role_distribution", "comfort"],
  "properties": {
    "product_id": { "type": "string", "minLength": 1 },
    "persona_id": { "type": "string", "minLength": 1 },
    "image_count": { "type": "integer", "minimum": 0 },
    "composition_top_k": {
      "type": "array",
      "items": {
        "type": "object", "additionalProperties": false,
        "required": ["value", "freq", "score"],
        "properties": { "value": { "type": "string" }, "freq": { "type": "number" }, "score": { "type": "number" } }
      }
    },
    "text_role_distribution": { "type": "object", "additionalProperties": { "type": "number" } },
    "hook_top_k": {
      "type": "array",
      "items": { "type": "object", "additionalProperties": false, "required": ["value", "freq", "score"],
        "properties": { "value": { "type": "string" }, "freq": { "type": "number" }, "score": { "type": "number" } } }
    },
    "copy_keywords_top_k": {
      "type": "array",
      "items": { "type": "object", "additionalProperties": false, "required": ["value", "freq", "score"],
        "properties": { "value": { "type": "string" }, "freq": { "type": "number" }, "score": { "type": "number" } } }
    },
    "medium_top_k": {
      "description": "Axis 3 — rendering medium (photo/illustration/flat_graphic/…) distribution from visual analyses.",
      "type": "array",
      "items": { "type": "object", "additionalProperties": false, "required": ["value", "freq", "score"],
        "properties": { "value": { "type": "string" }, "freq": { "type": "number" }, "score": { "type": "number" } } }
    },
    "setting_top_k": {
      "description": "Axis 3 — scene setting (studio/lifestyle/flatlay/…) distribution from visual analyses.",
      "type": "array",
      "items": { "type": "object", "additionalProperties": false, "required": ["value", "freq", "score"],
        "properties": { "value": { "type": "string" }, "freq": { "type": "number" }, "score": { "type": "number" } } }
    },
    "register_top_k": {
      "description": "Axis 4 — named visual register/mood (clean_minimal/premium_refined/raw_authentic/…) distribution.",
      "type": "array",
      "items": { "type": "object", "additionalProperties": false, "required": ["value", "freq", "score"],
        "properties": { "value": { "type": "string" }, "freq": { "type": "number" }, "score": { "type": "number" } } }
    },
    "appeal_top_k": {
      "description": "Axis 5 — persuasion appeal (price/social_proof/quality_proof/…) distribution from intent analyses (the transferable strategy layer).",
      "type": "array",
      "items": { "type": "object", "additionalProperties": false, "required": ["value", "freq", "score"],
        "properties": { "value": { "type": "string" }, "freq": { "type": "number" }, "score": { "type": "number" } } }
    },
    "funnel_stage_top_k": {
      "description": "Axis 5 — funnel stage (awareness/consideration/conversion/retargeting) distribution from intent analyses.",
      "type": "array",
      "items": { "type": "object", "additionalProperties": false, "required": ["value", "freq", "score"],
        "properties": { "value": { "type": "string" }, "freq": { "type": "number" }, "score": { "type": "number" } } }
    },
    "comfort": {
      "type": "object", "additionalProperties": false,
      "required": ["avg_crowding", "avg_whitespace", "awkward_rate"],
      "properties": { "avg_crowding": { "type": "number" }, "avg_whitespace": { "type": "number" }, "awkward_rate": { "type": "number" } }
    },
    "synthesis": { "type": "string" },
    "generated_at": { "type": "string" },
    "confidence_note": { "type": "string" }
  }
}
